#include <bits/stdc++.h>
using namespace std;

int N, R, C;
string S;

int main() {
    cin >> N;
    vector<pair<int, int>> old(N);
    old.push_back({0,0});
    for (int i = 1; i <= N; i++) {
        vector<pair<int, int>> neW(N);
        
    }
}